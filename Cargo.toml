[package]
name = "appguard-rust-clients"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = { version = "0.12.3", features = ["tls", "tls-roots"] }
prost = "0.13.4"
tokio = { version = "1.10.0", default-features = false, features = ["time"] }

[build-dependencies]
tonic-build = "0.12.3"
